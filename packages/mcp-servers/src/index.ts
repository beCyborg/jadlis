// @jadlis/mcp-servers — Custom MCP servers
// Implemented in section-09-mcp-servers
//
// This package exports nothing because each MCP server (supabase, polymarket,
// firecrawl) is launched as an independent subprocess via its own entry point.
// The barrel exists only to satisfy TypeScript's "module" requirement.
export {};
